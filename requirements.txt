# ─ Base UI / runtime
streamlit>=1.32

# ─ LLM client
openai>=1.14

# ─ Text-to-speech
elevenlabs>=1.0       # official ElevenLabs SDK
gTTS>=2.5             # fallback if ELEVEN_API_KEY isn’t set

# ─ .env convenience (optional but handy)
python-dotenv>=1.0
